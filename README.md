# Telegram-бот для лаборатории анализов

В проект уже встроен реальный файл `price_list.csv` с колонками:

```csv
code,name,result_type,max_discount,term_days,price,synonyms
```

Бот ищет анализы:

1. по коду анализа `code`;
2. по официальному названию `name`;
3. по синонимам `synonyms`;
4. через нечеткое совпадение RapidFuzz.

В выдаче пациенту и лаборатории показываются:

- код анализа;
- название;
- тип результата;
- срок выполнения;
- цена;
- максимальная скидка;
- примененная скидка;
- итоговая цена.

## Локальный запуск

1. Установите Python 3.11 или 3.12.
2. Откройте папку проекта в VS Code.
3. Создайте виртуальное окружение:

```powershell
python -m venv .venv
```

4. Активируйте его:

```powershell
.\.venv\Scripts\Activate.ps1
```

5. Установите зависимости:

```powershell
pip install -r requirements.txt
```

6. Создайте файл `.env` по примеру `.env.example`:

```env
BOT_TOKEN=токен_бота_от_BotFather
LAB_CHAT_ID=0
```

7. Запустите бота:

```powershell
python bot.py
```

## Railway

1. Загрузите проект в GitHub.
2. На Railway создайте New Project → Deploy from GitHub repo.
3. Выберите репозиторий.
4. В Variables добавьте:

```env
BOT_TOKEN=токен_бота_от_BotFather
LAB_CHAT_ID=0
```

5. Start Command:

```bash
python bot.py
```

6. После запуска добавьте бота в группу лаборатории, сделайте администратором и напишите в группе:

```text
/chatid
```

7. Скопируйте полученный chat_id и замените `LAB_CHAT_ID=0` на реальный ID в Railway Variables.
8. Выполните Redeploy.


## Колонка short_name в price_list.csv

Колонка `short_name` необязательная. Если она заполнена, бот использует это краткое название только на кнопках выбора вариантов. В итоговом списке и заказе остается полное официальное название анализа.


## Обновление v9

Добавлено:
- `alias_groups.csv` — файл групп соответствий для популярных запросов.
- В `price_list.csv` добавлены колонки `group` и `priority`.
- Бот сначала показывает готовый список найденных анализов.
- Варианты выбора появляются только после кнопки «Изменить часть».
- Есть кнопка «Удалить часть».
- Есть кнопка «Уточнить вручную» при выборе вариантов.

Формат `alias_groups.csv`:

```csv
alias,group
оак,cbc
общий анализ крови,cbc
оам,urine_general
```

Колонки в `price_list.csv`:
- `group` — группа анализа, например `cbc`.
- `priority` — приоритет внутри группы. Чем больше число, тем выше анализ в выдаче. Приоритет `100` будет выбран автоматически в готовый список.

## Обновление: голосовые сообщения и изображения через OpenAI

Бот теперь принимает:
- текстовый список анализов;
- голосовое сообщение;
- фото/скриншот/изображение со списком анализов;
- изображение, отправленное как файл-документ.

Для работы ИИ-слоя добавьте в `.env` локально или в Railway Variables:

```env
OPENAI_API_KEY=ваш_ключ_OpenAI
OPENAI_TEXT_MODEL=gpt-5-nano
OPENAI_VISION_MODEL=gpt-5.4-nano
OPENAI_AUDIO_MODEL=gpt-4o-mini-transcribe
```

Если `OPENAI_API_KEY` не задан, текстовый сценарий бота продолжит работать, но голос и изображения распознаваться не будут.

Если заказ был создан после голосового сообщения или изображения, в сообщение для группы лаборатории добавляется блок:

```text
ИИ-слой:
Источник: voice / image
Prompt/Input tokens: ...
Completion/Output tokens: ...
Total tokens: ...
```

Пациенту этот технический блок не отправляется.
